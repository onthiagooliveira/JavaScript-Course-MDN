/* 
  ? Operadores Lógicos

  Operadores lógicos são utlizados tipicamente com valores booleanos (lógicos), neste caso, retornam um valor booleano.
  Entretanto, os operadores && e || na verdade retornam o valor de um dos operandos especificados, de forma que se esses
  operadores forem ultizados com valores não booleanos, eles possam retornar um valor não booleano.

  ? &&  => AND Lógico
  Quando utlizado com valores booleanos (True or false), retorna verdadeiro caso ambos opeandos sejam verdadeiros; caso 
  contrario, retorna falso.

  ? ||  => OR Lógico
  Quando utilizado com valores booleanos, || retorna verdadeiro caso ambos os operandos sejam verdadeiro; se ambos forem 
  falsos, retorna falso.

  ? !  => NOT   Negação lógica 
  Retorna falso caso o único operado possa ser convertido para verdadeiro, se não retorna verdadeiro.

 !CONTINUAR ESTUDO À PATIR DO MINUTO 19.MN OPERADOR TERNÁRIO

  */
